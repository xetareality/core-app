{
  "scripts": {
    "start": "eleventy --serve & npx tailwindcss -i src/styles/tailwind.css -c tailwind.config.js -o dist/styles/tailwind.css --watch",
    "build": "ELEVENTY_PRODUCTION=true eleventy & NODE_ENV=production npx tailwindcss -i src/styles/tailwind.css -c tailwind.config.js -o dist/styles/tailwind.css --minify"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "alpinejs": "^3.3.3",
    "html-minifier": "^4.0.0"
  },
  "dependencies": {
    "tailwindcss": "^3.0.1"
  }
}
